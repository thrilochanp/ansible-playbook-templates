---
- name: Configure important user consistently
  hosts: localhost
  become: true
  become_method: su
  become_user: nobody
  tasks:
    - name: newbie exists with UID 4000
      user:
        name: newbie
        uid: 4000
        state: present
